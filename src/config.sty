\usepackage[acronym]{glossaries}
\glsdisablehyper
\loadglsentries{glossary}
%\makenoidxglossaries

\usepackage{biblatex}
\addbibresource{own.bib}
\addbibresource{references.bib}


\usepackage{booktabs}
\usepackage{caption}
\usepackage{array}

% Tables
\usepackage{tabularx}
\usepackage{multirow}
\usepackage{colortbl}

\usepackage[most]{tcolorbox}

\newcommand\blankfootnote[1]{%
  \let\thefootnote\relax\footnotetext{#1}%
  \let\thefootnote\svthefootnote%
}

% /!\ Require lualatex compiler
\usepackage{emoji}
\usepackage{tikz}
\usetikzlibrary{arrows}
\usetikzlibrary{calc}
\usetikzlibrary{shapes.geometric}

\newcommand{\hr}{\cellcolor{red!30}}
\newcommand{\ho}{\cellcolor{orange!30}}
\newcommand{\hg}{\cellcolor{imta-green!30}} 

% For every picture that defines or uses external nodes, you'll have to
% apply the 'remember picture' style. To avoid some typing, we'll apply
% the style to all pictures.
\tikzstyle{every picture}+=[remember picture]
\tikzstyle{na} = [baseline=-.5ex]



% \usepackage{keycommand}
% \usepackage{ifthen}

% \newkeycommand{\absolute}[pos=(0,0),debug=false][1]{
%     \tikz[overlay, remember picture,
%       shift=(current page.south west),
%       x=(current page.south east),
%       y=(current page.north west)
%     ]{
%       % Check if debug is true
%       \ifthenelse{\equal{\commandkey{debug}}{true}}{
%         % Show the optional help grid lines when debug is true
%         \draw[step=.1, opacity=0.3, thick, red] (0,0) grid (1,1);
%       }{}
      
%       % Place the node at the specified position
%       \node[align=left, anchor=south west] at (0,0) {#1};
%     }
% }